import { carService as toyService } from "../../services/car.service.js"


const initialState = {
}

export function toyReducer(state = initialState, action = {}) {
    switch (action.type) {
        default:
            return state
    }
}
